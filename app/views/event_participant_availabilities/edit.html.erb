<h1>Editing event_participant_availability</h1>

<% form_for(@event_participant_availability) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @event_participant_availability %> |
<%= link_to 'Back', event_participant_availabilities_path %>
